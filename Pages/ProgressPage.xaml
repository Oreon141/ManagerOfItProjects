<Page x:Class="ManagerOfItProjects.Pages.ProgressPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      Background="{StaticResource BackgroundBrush}"
      Loaded="Page_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок страницы -->
        <TextBlock Text="Мониторинг прогресса"
                   FontSize="36"
                   FontWeight="Bold"
                   Foreground="#1e293b"
                   Margin="0 0 0 20"/>

        <!-- Фильтр по проекту -->
        <Border Grid.Row="1" 
                Style="{StaticResource Card}" 
                Padding="20" 
                Margin="0 0 0 20">
            <StackPanel>
                <TextBlock Text="Проект" 
                           FontSize="20" 
                           Margin="0 0 0 6"/>
                <ComboBox x:Name="ProjectFilter"
                          VerticalContentAlignment="Center"
                          Background="#f1f5f9"
                          BorderBrush="#cbd5e1"
                          Height="42"
                          FontSize="18"
                          DisplayMemberPath="ProjectName"
                          SelectionChanged="ProjectFilter_Changed"/>
            </StackPanel>
        </Border>

        <!-- Карточка с диаграммами -->
        <Border Grid.Row="2" 
                Style="{StaticResource Card}" 
                Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>

                <!-- Кнопка "Предыдущая диаграмма" -->
                <Button Content="◀"
                        FontSize="32"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#475569"
                        Click="PrevChart_Click"
                        VerticalAlignment="Center"
                        Cursor="Hand"/>

                <!-- Область с диаграммами -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок текущей диаграммы -->
                    <TextBlock x:Name="ChartTitle"
                               Grid.Row="0"
                               FontSize="28"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               Margin="0 0 0 15"/>

                    <!-- Сообщение "Нет данных" -->
                    <TextBlock x:Name="NoDataMessage"
                               Grid.Row="1"
                               Text="Нет данных для отображения"
                               FontSize="32"
                               FontWeight="SemiBold"
                               Foreground="#94a3b8"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"/>

                    <!-- Круговая диаграмма -->
                    <lvc:PieChart x:Name="PieChart"
                                  Grid.Row="1"
                                  LegendLocation="Right"
                                  FontSize="18"
                                  Visibility="Collapsed">
                        <lvc:PieChart.RenderTransform>
                            <TranslateTransform/>
                        </lvc:PieChart.RenderTransform>
                    </lvc:PieChart>

                    <!-- Столбчатая диаграмма -->
                    <lvc:CartesianChart x:Name="BarChart"
                                        Grid.Row="1"
                                        FontSize="18"
                                        Visibility="Collapsed">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Пользователи"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Количество задач"/>
                        </lvc:CartesianChart.AxisY>
                        <lvc:CartesianChart.RenderTransform>
                            <TranslateTransform/>
                        </lvc:CartesianChart.RenderTransform>
                    </lvc:CartesianChart>

                    <!-- Диаграмма Ганта -->
                    <lvc:CartesianChart x:Name="GanttChart"
                                        Grid.Row="1"
                                        FontSize="18"
                                        Visibility="Collapsed">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Дни"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Этапы"/>
                        </lvc:CartesianChart.AxisY>
                        <lvc:CartesianChart.RenderTransform>
                            <TranslateTransform/>
                        </lvc:CartesianChart.RenderTransform>
                    </lvc:CartesianChart>
                </Grid>

                <!-- Кнопка "Следующая диаграмма" -->
                <Button Grid.Column="2"
                        Content="▶"
                        FontSize="32"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#475569"
                        Click="NextChart_Click"
                        VerticalAlignment="Center"
                        Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</Page>