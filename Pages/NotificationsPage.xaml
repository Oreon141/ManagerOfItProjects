<Page x:Class="ManagerOfItProjects.Pages.NotificationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000"
      Background="{StaticResource BackgroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Style="{StaticResource Card}" Padding="20">
            <DockPanel>
                <TextBlock Text="Уведомления" FontSize="28" FontWeight="Bold" Foreground="#1e293b" DockPanel.Dock="Left"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ComboBox x:Name="FilterBox" Width="180" Margin="0 0 10 0" SelectionChanged="FilterBox_SelectionChanged">
                        <ComboBoxItem Content="Все" IsSelected="True"/>
                        <ComboBoxItem Content="Непрочитанные"/>
                        <ComboBoxItem Content="Прочитанные"/>
                    </ComboBox>
                    <Button Content="Отметить всё прочитанным" Style="{StaticResource PrimaryButton}" Padding="16 8" Click="MarkAllRead_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <DataGrid x:Name="NotificationsGrid" AutoGenerateColumns="False" IsReadOnly="True" MouseDoubleClick="NotificationsGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Тип" Binding="{Binding TypeIcon}" Width="70"/>
                    <DataGridTextColumn Header="Заголовок" Binding="{Binding Title}" Width="180"/>
                    <DataGridTextColumn Header="Сообщение" Binding="{Binding Message}" Width="*"/>
                    <DataGridTextColumn Header="Дата" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy HH:mm}" Width="160"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding ReadStatus}" Width="130"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
