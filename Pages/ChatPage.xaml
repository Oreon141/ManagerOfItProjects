<Page x:Class="ManagerOfItProjects.Pages.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" Background="{StaticResource BackgroundBrush}">
    <Grid Margin="10">
        <Grid.ColumnDefinitions><ColumnDefinition Width="250"/><ColumnDefinition Width="*"/><ColumnDefinition Width="220"/></Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Button Content="+ Новый чат" Style="{StaticResource PrimaryButton}" Margin="0 0 0 10" Click="NewChat_Click"/>
                <ListBox x:Name="ChatsList" Grid.Row="1" DisplayMemberPath="DisplayName" SelectionChanged="ChatsList_SelectionChanged"/>
            </Grid>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <ListBox x:Name="MessagesList" Grid.Row="0"/>
                <DockPanel Grid.Row="1" Margin="0 10 0 0">
                    <TextBox x:Name="MessageBox" Height="40" Margin="0 0 10 0" VerticalContentAlignment="Center" KeyDown="MessageBox_KeyDown"/>
                    <Button Content="Отправить" Width="140" Style="{StaticResource PrimaryButton}" Click="Send_Click"/>
                </DockPanel>
            </Grid>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Участники" FontSize="20" FontWeight="Bold" Margin="0 0 0 10"/>
                <ListBox x:Name="ParticipantsList" DisplayMemberPath="DisplayText"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
